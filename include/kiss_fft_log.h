/* Stub kiss_fft_log.h — suppress output on Vita (no stdio needed) */
#ifndef KISS_FFT_LOG_H
#define KISS_FFT_LOG_H
#define KISS_FFT_LOG(...)   do {} while(0)
#define KISS_FFT_ERROR(...) do {} while(0)
#endif
